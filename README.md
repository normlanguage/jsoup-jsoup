# jsoup

适配声明与可运行示例位于 `jsoup/jsoup`，固定 jsoup 1.23.2，发布坐标为 `jsoup:jsoup:1`。公开面覆盖 HTML 解析、CSS 选择、DOM 查询与修改、输出设置、清洗和 HTTP 连接；API census 与未支持原因位于 NAR 的 `binding/java-api.json`。

独立 NAR 消费、真实 JAR、DOM 行为、Safelist 和本机 HTTP 验收由 `JsoupBindingIntegrationTest` 执行。
